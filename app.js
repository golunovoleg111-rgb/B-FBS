const AUTH_KEY='b-fbs-auth';const ADMIN_LOGIN='Admin1';const ADMIN_PASSWORD='Admin123';
const TABS=[['dashboard','Главный экран'],['workspace','Рабочее пространство'],['inventory','Учет склада'],['transfer','Заявка на перемещение'],['revision','Ревизия'],['account','Личный кабинет'],['tasks','Сборочное задание']];
const root=document.getElementById('root');let activeTab='dashboard';let workspaceMode='design';let tool='select';let pointerAction=null;let draft={walls:[],objects:[]};let published=null;let zones=[];
function loginView(){root.innerHTML=`<main class="login"><section class="card"><div class="login-mark">B</div><div class="eyebrow">B-FBS</div><h1>Вход в систему</h1><p class="desc">Складская рабочая система</p><form class="form" id="login-form"><label>Логин<input id="login" autocomplete="username" required></label><label>Пароль<input id="password" type="password" autocomplete="current-password" required></label><div id="login-error"></div><button class="btn primary" type="submit">Войти</button></form></section></main>`;document.getElementById('login-form').onsubmit=e=>{e.preventDefault();if(login.value===ADMIN_LOGIN&&password.value===ADMIN_PASSWORD){sessionStorage.setItem(AUTH_KEY,'admin');render()}else document.getElementById('login-error').innerHTML='<div class="error">Неверный логин или пароль</div>'}}
function dashboardView(){return `<div class="dashboard"><div class="welcome"><div><h2>Оперативная сводка</h2><p>Ключевые показатели склада за текущий момент.</p></div><div class="date">WB API: не подключен</div></div><div class="metrics"><div class="metric"><div class="metric-label">Заказы</div><div class="metric-value">0</div><div class="metric-note">Актуальные заказы на WB</div></div><div class="metric"><div class="metric-label">Готовые задания</div><div class="metric-value">0</div><div class="metric-note">Ожидают сборки</div></div><div class="metric"><div class="metric-label">Завершенные</div><div class="metric-value">0</div><div class="metric-note">Отгружены и закрыты</div></div><div class="metric"><div class="metric-label">Остатки</div><div class="metric-value">0</div><div class="metric-note">Единиц товара на складе</div></div></div><div class="panels"><div class="panel"><div class="panel-head"><div class="panel-title">Состояние сборки</div><div class="panel-link">Сегодня</div></div><div class="status-list"><div class="status-row"><span>В очереди</span><b>0</b></div><div class="status-row"><span>В работе</span><b>0</b></div><div class="status-row"><span>Готово к отгрузке</span><b>0</b></div></div></div><div class="panel"><div class="panel-head"><div class="panel-title">Последние события</div></div><div class="empty-panel">Событий пока нет</div></div></div></div>`}
function workspaceView(){const isDesign=workspaceMode==='design';const source=published||draft;return `<div class="workspace"><div class="workspace-head"><div><h2>${isDesign?'Проектирование склада':'Склад'}</h2><p>${isDesign?'Создайте визуальный макет: размеры, стены, входы, рабочие зоны, места для коробок и мусора.':'Опубликованная схема склада. Изменение схемы создаёт отдельную копию и не меняет отображение сотрудников.'}</p></div><div class="design-actions">${isDesign?'<span class="draft-badge">Черновик</span>':'<span class="saved-badge">Опубликованная схема</span>'}</div></div><div class="workspace-grid"><aside class="tool-panel panel">${isDesign?designPanel():managementPanel()}</aside><div class="canvas-wrap panel"><div class="canvas-title">План склада · ${isDesign?'режим проектирования':'рабочая схема'}</div><div class="floor" id="floor">${renderSvg(source)}${source.objects.map((o,i)=>`<div class="placed-object" data-object-id="${i}" style="left:${o.x}%;top:${o.y}%;width:${o.w}%;height:${o.h}%"><span>${o.name}</span><button class="remove-object" data-remove="${i}" aria-label="Удалить">×</button><i class="resize-handle"></i></div>`).join('')}${zones.map((z,i)=>`<div class="zone" style="left:${z.x}%;top:${z.y}%;width:${z.w}%;height:${z.h}%"><b>${z.name}</b><small>${z.capacity} ящиков</small><button data-zone="${i}">Открыть</button></div>`).join('')}${!source.walls.length&&!source.objects.length?'<div class="floor-note">Выберите «Линия» или «Прямоугольник»<br><small>и нарисуйте границы склада</small></div>':''}</div></div></div></div>`}
function designPanel(){return `<div class="panel-title">Этап проектирования · 01 Стены</div><p class="hint">Нарисуйте каркас склада. Линия — отдельная стена, прямоугольник — замкнутый контур.</p><div class="tool-buttons"><button class="tool ${tool==='select'?'active':''}" data-tool="select">Выбор</button><button class="tool ${tool==='line'?'active':''}" data-tool="line">Линия</button><button class="tool ${tool==='rectangle'?'active':''}" data-tool="rectangle">Прямоугольник</button></div><div class="legend"><span>ЛКМ + движение — рисование</span><span>Выбор — перемещение объектов</span><span>Перетаскивание за угол — изменение размера объекта</span></div><div class="object-section"><div class="panel-title">Этап проектирования · 02 Объекты</div><p class="hint">Добавьте элементы прямо на план. Их можно перемещать и изменять размер.</p><div class="object-buttons">${['Вход','Рабочая зона сборщиков','Место для коробок','Место для мусора','Окно','Ворота','Перегородка'].map(x=>`<button class="object-btn" data-object="${x}">${x}</button>`).join('')}</div></div><button class="btn primary wide" id="save-design">Сохранить изменения</button>`}
function managementPanel(){return `<div class="panel-title">Управление складом</div><p class="hint">Проектирование завершено. Сотрудники видят опубликованную схему. Только администратор может создать её новую копию.</p><button class="btn wide" id="edit-copy">Создать копию для редактирования</button><div class="object-section"><div class="panel-title">Зоны хранения</div><p class="hint">Теперь доступны инструменты размещения зон. Зоны не являются частью этапа проектирования.</p><div class="zone-form"><label>Название зоны<input id="zone-name" placeholder="Например, Зона A"></label><label>Вместимость ящиков<input id="zone-capacity" type="number" min="1" placeholder="100"></label><button class="btn primary wide" id="add-zone">Добавить зону</button></div></div>`}
function renderSvg(source){return `<svg class="warehouse-svg" viewBox="0 0 1000 500" preserveAspectRatio="none">${source.walls.map(w=>w.type==='line'?`<line x1="${w.x1}" y1="${w.y1}" x2="${w.x2}" y2="${w.y2}" />`:`<rect x="${w.x}" y="${w.y}" width="${w.w}" height="${w.h}" />`).join('')}</svg>`}
function shell(){root.innerHTML=`<div class="app"><aside class="side"><div class="brand"><div class="mark">B</div><div><div class="name">B-FBS</div><div class="sub">Складская система</div></div></div><nav class="nav" id="nav"></nav><div class="foot">● Система готова</div></aside><main class="main"><header class="top"><div><div class="eyebrow">B-FBS / РАБОЧАЯ СРЕДА</div><div class="title" id="title"></div></div><div class="user"><span class="avatar">A</span><span>Admin1</span><button id="api" class="btn">API Интеграция</button><button id="logout" class="btn">Выйти</button></div></header><section class="content" id="content"></section></main></div>`;TABS.forEach(([id,label],i)=>{const b=document.createElement('button');b.className=`nav-item ${activeTab===id?'active':''}`;b.innerHTML=`<span class="num">${String(i+1).padStart(2,'0')}</span><span>${label}</span>`;b.onclick=()=>{activeTab=id;render()};nav.appendChild(b)});title.textContent=TABS.find(x=>x[0]===activeTab)[1];logout.onclick=()=>{sessionStorage.removeItem(AUTH_KEY);render()};api.onclick=()=>alert('API Интеграция: подключение ключа WB и синхронизация будут добавлены отдельным этапом.');content.innerHTML=activeTab==='dashboard'?dashboardView():activeTab==='workspace'?workspaceView():`<div class="empty-state panel"><div class="empty-icon">${String(TABS.findIndex(x=>x[0]===activeTab)+1).padStart(2,'0')}</div><h2>${TABS.find(x=>x[0]===activeTab)[1]}</h2><p>Раздел подготовлен. Функциональность будет добавляться поэтапно.</p></div>`;if(activeTab==='workspace')bindWorkspace()}
function point(e,el){const r=el.getBoundingClientRect();return{x:Math.max(0,Math.min(100,(e.clientX-r.left)/r.width*100)),y:Math.max(0,Math.min(100,(e.clientY-r.top)/r.height*100))}}
function bindWorkspace(){const floor=document.getElementById('floor');document.querySelectorAll('[data-tool]').forEach(b=>b.onclick=()=>{tool=b.dataset.tool;render()});if(workspaceMode==='design'){floor.onpointerdown=e=>{if(e.target.closest('.placed-object')||tool==='select')return;const p=point(e,floor);pointerAction={mode:tool,start:p};floor.setPointerCapture(e.pointerId)};floor.onpointerup=e=>{if(!pointerAction)return;const p=point(e,floor),x=Math.min(pointerAction.start.x,p.x),y=Math.min(pointerAction.start.y,p.y),w=Math.abs(p.x-pointerAction.start.x),h=Math.abs(p.y-pointerAction.start.y);if(pointerAction.mode==='line'&&w+h>2)draft.walls.push({type:'line',x1:pointerAction.start.x*10,y1:pointerAction.start.y*5,x2:p.x*10,y2:p.y*5});if(pointerAction.mode==='rectangle'&&w>2&&h>2)draft.walls.push({type:'rect',x:x*10,y:y*5,w:w*10,h:h*5});pointerAction=null;render()};document.querySelectorAll('[data-object]').forEach(b=>b.onclick=()=>{draft.objects.push({name:b.dataset.object,x:8+(draft.objects.length%4)*22,y:12+Math.floor(draft.objects.length/4)*22,w:18,h:11});render()});document.querySelectorAll('[data-remove]').forEach(b=>b.onclick=e=>{e.stopPropagation();draft.objects.splice(Number(b.dataset.remove),1);render()});bindObjectDrag(floor,draft.objects);document.getElementById('save-design').onclick=()=>{published=JSON.parse(JSON.stringify(draft));workspaceMode='management';render()}}else{document.getElementById('edit-copy').onclick=()=>{draft=JSON.parse(JSON.stringify(published));workspaceMode='design';render()};document.getElementById('add-zone').onclick=()=>{const name=document.getElementById('zone-name').value.trim(),capacity=Number(document.getElementById('zone-capacity').value);if(!name||!capacity)return alert('Укажите название зоны и вместимость.');const i=zones.length;zones.push({name,capacity,x:5+(i%3)*30,y:8+Math.floor(i/3)*25,w:22,h:18});render()}}}
function bindObjectDrag(floor,objects){document.querySelectorAll('[data-object-id]').forEach(el=>{el.onpointerdown=e=>{if(e.target.closest('button'))return;const i=Number(el.dataset.objectId),p=point(e,floor),o=objects[i];const resize=e.target.closest('.resize-handle');pointerAction={mode:resize?'resize':'move',index:i,dx:p.x-o.x,dy:p.y-o.y,startW:o.w,startH:o.h,startX:p.x,startY:p.y};el.setPointerCapture(e.pointerId)};el.onpointermove=e=>{if(!pointerAction||pointerAction.index!==Number(el.dataset.objectId))return;const p=point(e,floor),o=objects[pointerAction.index];if(pointerAction.mode==='move'){o.x=Math.max(0,Math.min(100-o.w,p.x-pointerAction.dx));o.y=Math.max(0,Math.min(100-o.h,p.y-pointerAction.dy));el.style.left=o.x+'%';el.style.top=o.y+'%'}else{o.w=Math.max(5,Math.min(100-o.x,pointerAction.startW+(p.x-pointerAction.startX)));o.h=Math.max(5,Math.min(100-o.y,pointerAction.startH+(p.y-pointerAction.startY)));el.style.width=o.w+'%';el.style.height=o.h+'%'}};el.onpointerup=()=>{pointerAction=null}})}
function render(){if(sessionStorage.getItem(AUTH_KEY)!=='admin')loginView();else shell()}render();
